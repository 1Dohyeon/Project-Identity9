import AuthLayout from "../features/auth/components/auth.layout";
import LoginComponent from "../features/auth/components/Login.form.component";

const LoginPage = () => {
  return (
    <AuthLayout>
      <LoginComponent />
    </AuthLayout>
  );
};

export default LoginPage;
